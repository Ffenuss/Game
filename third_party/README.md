# Third Party Assets

Store source downloads here only.

Rules:

- Keep original source files untouched.
- Never reference `third_party/` paths directly from scenes or scripts.
- Normalize selected files into `assets/runtime/`.
- Keep license proof beside each imported source file.

